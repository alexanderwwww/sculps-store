{%- comment -%}
============================================================================
  HELIOS SUPER GLIDE — BUY BOX  (product-first, conversion-optimized)
  Drop into: sections/helios-buy-box.liquid  →  add to product template.
  ----------------------------------------------------------------------------
  • Real Shopify buy form: {% form 'product', product %} + {{ form | payment_button }}
    renders Apple Pay / Shop Pay / Google Pay express buttons.
  • Bundle tiers = section BLOCKS, each pointing at a REAL variant ID, so each
    tier adds the correct variant. Prices pulled live via {{ variant.price | money }}.
  • Lead media = demo video (autoplay muted loop playsinline, poster fallback),
    with gallery thumbs below. Hero video vs image is a setting (A/B).
  • Countdown (per-session, honest), low-stock, live viewers, recent-purchase toasts.
  HOW TO WIRE VARIANTS:
    Online Store → Theme editor → this section → each "Bundle tier" block →
    paste the numeric Variant ID (Products → variant → the number at the end of
    the admin URL …/variants/<ID>). qty = how many units that variant represents.
============================================================================
{%- endcomment -%}

{%- unless product -%}{%- assign product = section.settings.featured_product -%}{%- endunless -%}
{%- assign cur = product.selected_or_first_available_variant -%}
{%- assign sid = section.id -%}
{%- capture hb_flag -%}<svg class="hb-flag" viewBox="0 0 20 14" xmlns="http://www.w3.org/2000/svg"><rect width="20" height="14" fill="#B22234"/><rect y="2" width="20" height="2" fill="#fff"/><rect y="6" width="20" height="2" fill="#fff"/><rect y="10" width="20" height="2" fill="#fff"/><rect width="9" height="8" fill="#3C3B6E"/><g fill="#fff"><circle cx="2" cy="2" r=".7"/><circle cx="4.5" cy="2" r=".7"/><circle cx="7" cy="2" r=".7"/><circle cx="3.25" cy="4" r=".7"/><circle cx="5.75" cy="4" r=".7"/><circle cx="2" cy="6" r=".7"/><circle cx="4.5" cy="6" r=".7"/><circle cx="7" cy="6" r=".7"/></g></svg>{%- endcapture -%}

<section class="helios helios-buy" id="hb-{{ sid }}">
<style>
  #hb-{{ sid }} .hb-wrap{max-width:1200px;margin:0 auto;padding:clamp(16px,3vw,40px) 22px clamp(32px,5vw,56px);}
  #hb-{{ sid }} .hb-grid{display:grid;grid-template-columns:1fr;gap:30px;}
  @media(min-width:880px){
    #hb-{{ sid }} .hb-grid{grid-template-columns:1.04fr .96fr;gap:46px;align-items:start;}
    #hb-{{ sid }} .hb-gallery{position:sticky;top:90px;}
  }
  #hb-{{ sid }} .hb-stage{position:relative;display:flex;overflow-x:auto;overflow-y:hidden;touch-action:pan-x;scroll-snap-type:x mandatory;scroll-behavior:smooth;border-radius:24px;background:linear-gradient(160deg,#EAF6FB,#D6ECF5);border:var(--helios-g-bd,1px solid rgba(255,255,255,.65));box-shadow:var(--helios-g-shlg,0 26px 60px -18px rgba(6,47,73,.3));aspect-ratio:1/1;scrollbar-width:none;-webkit-overflow-scrolling:touch;overscroll-behavior-x:contain;}
  #hb-{{ sid }} .hb-cell{flex:0 0 100%;scroll-snap-align:center;display:grid;place-items:center;position:relative;height:100%;overflow:hidden;}
  #hb-{{ sid }} .hb-stage::-webkit-scrollbar{display:none;}
  #hb-{{ sid }} .hb-cell{flex:0 0 100%;scroll-snap-align:center;display:grid;place-items:center;position:relative;}
  #hb-{{ sid }} .hb-cell > img,#hb-{{ sid }} .hb-cell > video{width:100%;height:100%;object-fit:contain;display:block;}
  #hb-{{ sid }} .hb-dots{display:flex;gap:6px;justify-content:center;margin-top:11px;min-height:7px;}
  #hb-{{ sid }} .hb-dot{width:7px;height:7px;border-radius:50%;background:rgba(12,26,36,.18);transition:.2s;}
  #hb-{{ sid }} .hb-dot.on{background:var(--helios-acc,#0E7CC4);width:20px;border-radius:4px;}
  #hb-{{ sid }} .hb-pk{flex:1;display:flex;align-items:center;gap:11px;text-align:left;cursor:pointer;border:2px solid rgba(12,26,36,.14);background:#fff;border-radius:14px;padding:10px 13px;transition:.18s;}
  #hb-{{ sid }} .hb-pk .hb-pkimg{flex:0 0 46px;width:46px;height:46px;border-radius:10px;object-fit:cover;background:linear-gradient(160deg,#EAF6FB,#D6ECF5);}
  #hb-{{ sid }} .hb-pk .hb-pktx{display:flex;flex-direction:column;min-width:0;}
  #hb-{{ sid }} .hb-pk b{display:block;font-family:'Archivo',sans-serif;font-weight:800;font-size:14px;color:var(--helios-ink,#0C1A24);}
  #hb-{{ sid }} .hb-pk span{font-size:11.5px;color:var(--helios-mut,#56636E);}
  #hb-{{ sid }} .hb-pk.on{background:#0A1219;border-color:#0A1219;box-shadow:0 12px 26px rgba(6,30,46,.30);}
  #hb-{{ sid }} .hb-pk.on b{color:#fff;}
  #hb-{{ sid }} .hb-pk.on span{color:rgba(255,255,255,.72);}
  #hb-{{ sid }} .hb-gallery{position:relative;}
  #hb-{{ sid }} .hb-dots{display:none;}
  #hb-{{ sid }} .hb-thumbs{position:absolute;left:0;right:0;bottom:12px;z-index:4;display:flex;gap:7px;justify-content:center;align-items:center;padding:0 12px;margin:0;overflow-x:auto;scrollbar-width:none;-webkit-overflow-scrolling:touch;}
  #hb-{{ sid }} .hb-thumbs::-webkit-scrollbar{display:none;}
  #hb-{{ sid }} .hb-thumb{position:relative;padding:3px;flex:0 0 auto;width:clamp(32px,8.5%,42px);aspect-ratio:1/1;border:1.5px solid rgba(255,255,255,.7);border-radius:11px;overflow:hidden;cursor:pointer;background:rgba(255,255,255,.15);backdrop-filter:blur(12px) saturate(160%);-webkit-backdrop-filter:blur(12px) saturate(160%);box-shadow:0 3px 10px rgba(6,30,46,.18);opacity:.55;transition:opacity .28s ease,width .34s cubic-bezier(.34,1.56,.64,1),transform .28s ease,box-shadow .28s ease,border-color .28s ease;}
  #hb-{{ sid }} .hb-thumb:hover{opacity:.9;transform:translateY(-2px);}
  #hb-{{ sid }} .hb-thumb.sel{opacity:1;width:clamp(52px,14%,66px);border:2px solid #fff;box-shadow:0 8px 24px rgba(6,30,46,.45);animation:hb-thumb-live 2.6s ease-in-out infinite;}
  @keyframes hb-thumb-live{0%,100%{transform:translateY(0) scale(1);}50%{transform:translateY(-2px) scale(1.045);}}
  @media(prefers-reduced-motion:reduce){ #hb-{{ sid }} .hb-thumb.sel{animation:none;} }
  #hb-{{ sid }} .hb-thumb img,#hb-{{ sid }} .hb-thumb video{width:100%;height:100%;object-fit:contain;display:block;border-radius:8px;}
  #hb-{{ sid }} .hb-ey{display:inline-flex;align-items:center;gap:7px;background:#0C1A24;color:#fff;font-size:11.5px;font-weight:700;padding:7px 13px;border-radius:30px;margin-bottom:14px;}
  #hb-{{ sid }} h1{font-weight:800;font-size:clamp(28px,4.4vw,42px);line-height:1.04;letter-spacing:-.02em;margin:0;}
  #hb-{{ sid }} .hb-sub{color:#5B6470;font-size:15px;line-height:1.5;margin:8px 0 0;max-width:480px;}
  #hb-{{ sid }} .hb-eyebrow{font-size:12px;font-weight:600;letter-spacing:.08em;text-transform:uppercase;color:#3E5568;margin-bottom:5px;}
  #hb-{{ sid }} .hb-pills{display:flex;flex-wrap:wrap;gap:6px;margin:11px 0 0;}
  #hb-{{ sid }} .hb-pill{display:inline-flex;align-items:center;gap:5px;font-size:11px;font-weight:600;color:#1F6B4A;background:rgba(74,190,140,.14);border:1px solid rgba(74,190,140,.38);border-radius:100px;padding:4px 10px;}
  #hb-{{ sid }} .hb-pill::before{content:"✓";font-size:10px;}
  #hb-{{ sid }} .hb-rate{display:flex;align-items:center;flex-wrap:wrap;gap:5px 8px;margin:12px 0 18px;font-size:13.5px;color:#5B6470;text-decoration:none;}
  #hb-{{ sid }} .hb-rate .hb-metadot{color:#c5ccd4;}
  #hb-{{ sid }} .hb-rate .hb-metaspec{color:#5B6470;font-weight:500;}
  #hb-{{ sid }} .hb-priceline{display:flex;align-items:baseline;gap:10px;flex-wrap:wrap;margin:0 0 18px;}
  #hb-{{ sid }} .hb-price-main{font-family:'Archivo',sans-serif;font-weight:800;font-size:32px;letter-spacing:-.02em;color:#0A0D11;}
  #hb-{{ sid }} .hb-priceline s{font-size:18px;color:#9aa2ab;font-weight:600;}
  #hb-{{ sid }} .hb-savesub{font-size:12.5px;font-weight:700;color:#16a34a;}
  #hb-{{ sid }} .hb-top{display:flex;align-items:flex-start;gap:13px;margin:2px 0 9px;}
  #hb-{{ sid }} .hb-top-left{flex:1 1 auto;min-width:0;}
  #hb-{{ sid }} .hb-top-div{flex:0 0 auto;align-self:stretch;width:1px;background:rgba(20,26,32,.12);margin:4px 0;}
  #hb-{{ sid }} .hb-top-right{flex:0 0 auto;display:flex;flex-direction:column;align-items:center;gap:7px;padding-top:4px;}
  #hb-{{ sid }} .hb-price{display:flex;align-items:baseline;gap:10px;flex-wrap:wrap;margin:0;}
  #hb-{{ sid }} .hb-price b{font-family:'Archivo',sans-serif;font-weight:800;font-size:clamp(33px,8.6vw,44px);line-height:1;letter-spacing:-.02em;color:#E11D2A;}
  #hb-{{ sid }} .hb-price s{font-size:clamp(17px,4.4vw,21px);color:#9aa2ab;font-weight:600;}
  #hb-{{ sid }} .hb-bundlepill{display:inline-flex;align-items:center;gap:6px;margin-top:7px;font-family:'Archivo',sans-serif;font-weight:800;font-size:11px;letter-spacing:.03em;text-transform:uppercase;color:#fff;background:linear-gradient(180deg,#2b2f36,#0a0d11);padding:7px 12px;border-radius:30px;box-shadow:0 4px 10px rgba(10,13,17,.28);white-space:nowrap;max-width:100%;}
  #hb-{{ sid }} .hb-autodisc{display:flex;align-items:flex-start;gap:8px;margin-top:6px;}
  #hb-{{ sid }} .hb-autodisc-ck{flex:0 0 auto;width:20px;height:20px;border-radius:50%;background:#16a34a;display:grid;place-items:center;margin-top:1px;}
  #hb-{{ sid }} .hb-autodisc-ck svg{width:12px;height:12px;stroke:#fff;}
  #hb-{{ sid }} .hb-autodisc b{display:block;font-family:'Archivo',sans-serif;font-weight:800;font-size:14px;color:#15803d;line-height:1.25;}
  #hb-{{ sid }} .hb-autodisc span{display:block;font-size:12.5px;color:#8a929c;}
  #hb-{{ sid }} .hb-savebox{font-family:'Archivo',sans-serif;font-weight:800;font-size:clamp(16px,4.6vw,20px);color:#15803d;background:#daf3e3;padding:9px 15px;border-radius:12px;white-space:nowrap;text-align:center;letter-spacing:-.01em;}
  #hb-{{ sid }} .hb-savesub{font-size:13px;font-weight:800;color:#16a34a;white-space:nowrap;}
  #hb-{{ sid }} .hb-shipbox{display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:5px 14px;margin:0 0 12px;padding:9px 13px;background:#fff;border:1px solid rgba(20,26,32,.12);border-radius:12px;box-shadow:0 4px 12px -8px rgba(10,20,30,.1);}
  #hb-{{ sid }} .hb-shipcol{display:inline-flex;align-items:center;gap:6px;font-size:12.5px;color:#3b444d;min-width:0;}
  #hb-{{ sid }} .hb-shipcol svg{flex:0 0 auto;color:#0A0D11;}
  #hb-{{ sid }} .hb-shipcol.ok svg{color:#16a34a;}
  #hb-{{ sid }} .hb-shipcol b{font-family:'Archivo',sans-serif;font-weight:800;color:#0A0D11;white-space:nowrap;}
  #hb-{{ sid }} .hb-shipcol.ok b{color:#16a34a;}
  #hb-{{ sid }} .hb-shipcol i{font-style:normal;color:#8a929c;white-space:nowrap;}
  #hb-{{ sid }} .hb-1seller{margin:0 0 14px;border-radius:14px;padding:12px 13px;background:#fff;border:1px solid rgba(20,26,32,.10);box-shadow:0 8px 20px -12px rgba(10,20,30,.12);}
  #hb-{{ sid }} .hb-promo4{display:flex;align-items:center;gap:12px;margin:2px 0 12px;border-radius:14px;padding:11px 13px;background:#fff;border:1px solid rgba(20,26,32,.10);box-shadow:0 6px 16px -8px rgba(10,20,30,.12);}
  #hb-{{ sid }} .hb-promo4-main{flex:1 1 auto;min-width:0;display:flex;flex-direction:column;gap:6px;}
  #hb-{{ sid }} .hb-promo4-row1{display:flex;align-items:center;gap:8px;flex-wrap:wrap;}
  #hb-{{ sid }} .hb-promo4-tag{display:inline-flex;align-items:center;gap:6px;font-family:'Archivo',sans-serif;font-weight:800;font-size:10px;letter-spacing:.03em;color:#fff;background:linear-gradient(180deg,#2b2f36,#0a0d11);padding:6px 10px;border-radius:30px;box-shadow:0 4px 10px rgba(10,13,17,.3),inset 0 1px 0 rgba(255,255,255,.12);white-space:nowrap;}
  #hb-{{ sid }} .hb-promo4-off{font-family:'Archivo',sans-serif;font-weight:800;font-size:clamp(20px,5.4vw,26px);line-height:1;letter-spacing:-.01em;color:#E11D2A;white-space:nowrap;}
  #hb-{{ sid }} .hb-flag{height:1.15em;width:auto;border-radius:1.5px;flex:0 0 auto;display:block;box-shadow:0 0 0 .5px rgba(255,255,255,.25);}
  #hb-{{ sid }} .hb-promo4-applied{display:inline-flex;align-items:center;gap:5px;font-family:'Archivo',sans-serif;font-weight:800;font-size:10px;letter-spacing:.03em;text-transform:uppercase;color:#0f8a4d;background:#e8f7ef;padding:5px 9px;border-radius:20px;white-space:nowrap;}
  #hb-{{ sid }} .hb-promo4-sub{font-size:11.5px;line-height:1.35;color:#6b7480;}
  #hb-{{ sid }} .hb-promo4-div{flex:0 0 auto;width:1px;align-self:stretch;background:rgba(20,26,32,.12);}
  #hb-{{ sid }} .hb-promo4-ends{flex:0 0 auto;display:inline-flex;align-items:center;gap:7px;font-size:11.5px;font-weight:600;color:#3b444d;white-space:nowrap;}
  #hb-{{ sid }} .hb-promo4-ends b{display:block;font-weight:800;color:#0A0D11;}
  #hb-{{ sid }} .hb-promo4-ends svg{color:#8a929c;flex:0 0 auto;}
  @media(max-width:430px){
    #hb-{{ sid }} .hb-promo4{gap:9px;padding:10px 11px;}
    #hb-{{ sid }} .hb-promo4-off{font-size:21px;}
    #hb-{{ sid }} .hb-promo4-applied{font-size:9px;padding:4px 8px;}
    #hb-{{ sid }} .hb-promo4-sub{font-size:10.5px;}
    #hb-{{ sid }} .hb-promo4-ends{font-size:10.5px;gap:5px;}
  }
  #hb-{{ sid }} .hb-1seller-pill{display:inline-flex;align-items:center;gap:6px;font-family:'Archivo',sans-serif;font-weight:800;font-size:10px;letter-spacing:.03em;text-transform:uppercase;color:#fff;background:linear-gradient(180deg,#2b2f36,#0a0d11);padding:6px 11px;border-radius:30px;box-shadow:0 4px 10px rgba(10,13,17,.28),inset 0 1px 0 rgba(255,255,255,.12);}
  #hb-{{ sid }} .hb-1seller-vf{display:flex;align-items:center;gap:5px;margin-top:9px;font-size:11px;font-weight:600;color:#6b7480;}
  #hb-{{ sid }} .hb-1seller-tx b{display:block;font-family:'Archivo',sans-serif;font-weight:800;font-size:14.5px;color:#0A0D11;margin:8px 0 4px;letter-spacing:-.01em;}
  #hb-{{ sid }} .hb-1seller-tx p{margin:0;font-size:12px;color:#6b7480;line-height:1.45;}
  #hb-{{ sid }} .hb-1seller-tx s{color:#e23b3b;font-weight:700;}
  #hb-{{ sid }} .hb-1seller-tx em{font-style:normal;font-weight:800;color:#0A0D11;}
  #hb-sticky-{{ sid }}{position:fixed;left:14px;right:82px;bottom:16px;z-index:9998;display:flex;align-items:center;gap:11px;padding:8px 9px 8px 12px;background:rgba(255,255,255,.40);backdrop-filter:blur(24px) saturate(185%);-webkit-backdrop-filter:blur(24px) saturate(185%);border:1px solid rgba(255,255,255,.55);border-radius:26px;box-shadow:0 16px 36px rgba(6,30,46,.22),inset 0 1px 0 rgba(255,255,255,.6);transform:translateY(calc(100% + 30px));transition:transform .42s cubic-bezier(.2,.8,.2,1);}
  #hb-sticky-{{ sid }}.show{transform:translateY(0);}
  #hb-sticky-{{ sid }} .hb-sk-info{display:flex;align-items:center;gap:10px;flex:1;min-width:0;}
  #hb-sticky-{{ sid }} .hb-sk-thumb{width:40px;height:40px;flex-shrink:0;border-radius:13px;object-fit:cover;border:1px solid rgba(255,255,255,.6);}
  #hb-sticky-{{ sid }} .hb-sk-name{font-family:'Archivo',sans-serif;font-weight:800;font-size:13.5px;line-height:1.1;color:#0C1A24;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
  #hb-sticky-{{ sid }} .hb-sk-price b{font-family:'Archivo',sans-serif;font-weight:800;font-size:13px;color:#0C1A24;}
  #hb-sticky-{{ sid }} .hb-sk-add{flex-shrink:0;background:#0A1219;color:#fff;border:none;border-radius:30px;padding:12px 26px;font-family:'Archivo',sans-serif;font-weight:800;font-size:14.5px;cursor:pointer;box-shadow:0 10px 22px rgba(10,18,25,.34);}
  #hb-{{ sid }} .hb-chips{display:flex;flex-wrap:wrap;gap:6px;margin:10px 0 2px;}
  #hb-{{ sid }} .hb-chip{font-size:11.5px;font-weight:700;color:var(--helios-ink,#0C1A24);background:rgba(14,124,196,.08);border:1px solid rgba(14,124,196,.16);border-radius:30px;padding:5px 10px;white-space:nowrap;}
  #hb-{{ sid }} .hb-timer{display:inline-flex;align-items:center;gap:7px;font-family:'Archivo';font-weight:800;font-size:12px;letter-spacing:.06em;text-transform:uppercase;color:var(--helios-accd,#0A5E97);background:var(--helios-g-bg,rgba(255,255,255,.55));border:var(--helios-g-bd2,1px solid rgba(255,255,255,.45));backdrop-filter:var(--helios-g-blur);padding:7px 12px;border-radius:9px;margin-bottom:16px;}
  #hb-{{ sid }} .hb-tiers{display:flex;flex-direction:column;gap:12px;background:transparent;border:none;padding:0;box-shadow:none;}
  #hb-{{ sid }} .hb-tier{display:flex;align-items:center;gap:13px;border:1.5px solid #E4E8EC;border-radius:16px;padding:16px;cursor:pointer;position:relative;background:#fff;transition:.16s;margin:0;}
  #hb-{{ sid }} .hb-tier.sel{border-color:#3E5568;border-width:2px;background:#fff;}
  #hb-{{ sid }} .hb-radio{width:22px;height:22px;border-radius:50%;border:2px solid #C7CED6;flex-shrink:0;transition:.2s;background:#fff;}
  #hb-{{ sid }} .hb-tier.sel .hb-radio{border:6.5px solid #3E5568;}
  #hb-{{ sid }} .hb-badge{position:absolute;top:-11px;left:16px;font-size:10px;font-weight:700;letter-spacing:.01em;text-transform:lowercase;color:#fff;padding:4px 12px;border-radius:20px;background:#3E5568;box-shadow:0 5px 12px rgba(62,85,104,.3);}
  #hb-{{ sid }} .hb-badge.teal{background:#3E5568;}
  #hb-{{ sid }} .hb-tiername{font-family:'Archivo';font-weight:800;font-size:16px;color:#0A0D11;}
  #hb-{{ sid }} .hb-tiersub{display:block;font-size:12.5px;color:#5B6470;font-weight:500;margin-top:2px;}
  #hb-{{ sid }} .hb-tierper{display:block;font-size:11.5px;color:#9aa2ab;font-weight:500;margin-top:3px;}
  #hb-{{ sid }} .hb-tierprice{text-align:right;display:flex;flex-direction:column;align-items:flex-end;gap:2px;}
  #hb-{{ sid }} .hb-tierprice b{font-family:'Archivo';font-weight:800;font-size:19px;color:#0A0D11;letter-spacing:-.01em;}
  #hb-{{ sid }} .hb-tierprice s{font-size:12.5px;color:#b3bac2;font-weight:600;}
  #hb-{{ sid }} .hb-atc{display:flex;width:100%;align-items:center;justify-content:center;gap:9px;background:#0A0D11;color:#fff;border:none;border-radius:100px;padding:17px;font-family:'Archivo';font-weight:800;font-size:16px;text-transform:lowercase;cursor:pointer;box-shadow:0 14px 30px rgba(10,13,17,.28);transition:transform .15s,background .15s;margin-top:16px;}
  #hb-{{ sid }} .hb-atc:hover{transform:translateY(-1px);background:#1a1f26;}
  #hb-{{ sid }} .hb-atc svg{width:18px;height:18px;}
  #hb-{{ sid }} .hb-atc:disabled,#hb-{{ sid }} .hb-buynow:disabled{opacity:.45;cursor:not-allowed;}
  #hb-{{ sid }} .hb-buynow{display:flex;width:100%;align-items:center;justify-content:center;background:#fff;color:#0A0D11;border:1.6px solid #0A0D11;border-radius:100px;padding:16px;font-family:'Archivo';font-weight:800;font-size:16px;text-transform:lowercase;letter-spacing:.01em;cursor:pointer;transition:transform .15s,background .15s;margin-top:11px;}
  #hb-{{ sid }} .hb-buynow:hover{transform:translateY(-1px);background:#f4f5f6;}
  #hb-{{ sid }} .hb-add .hb-sheen{position:absolute;top:0;left:0;width:40%;height:100%;background:linear-gradient(90deg,transparent,rgba(255,255,255,.45),transparent);animation:helios-sheen 4.5s ease-in-out infinite;}
  #hb-{{ sid }} .hb-pay{margin-top:11px;}
  #hb-{{ sid }} .hb-pay .shopify-payment-button__button{border-radius:14px !important;font-family:'Archivo',sans-serif !important;font-weight:800 !important;}
  #hb-{{ sid }} .hb-code{display:flex;align-items:center;justify-content:space-between;gap:10px;width:100%;margin-top:11px;background:var(--helios-g-bg,rgba(255,255,255,.55));border:1px dashed var(--helios-acc,#0E7CC4);backdrop-filter:var(--helios-g-blur);border-radius:13px;padding:12px 15px;cursor:pointer;transition:.2s;}
  #hb-{{ sid }} .hb-code b{font-family:'Archivo';font-weight:800;font-size:14px;color:var(--helios-accd,#0A5E97);letter-spacing:.04em;display:inline-flex;gap:8px;align-items:center;}
  #hb-{{ sid }} .hb-trust{display:flex;align-items:center;gap:6px 4px;justify-content:space-between;margin-top:11px;font-size:12px;color:var(--helios-mut,#56636E);text-align:center;flex-wrap:wrap;}
  #hb-{{ sid }} .hb-trust span{display:inline-flex;align-items:center;gap:5px;white-space:nowrap;flex:1;justify-content:center;min-width:88px;}
  #hb-{{ sid }} .hb-pays{display:flex;gap:7px;justify-content:center;margin-top:8px;opacity:.72;flex-wrap:wrap;}
  #hb-{{ sid }} .hb-pays span{font-size:10px;font-weight:700;border:1px solid var(--helios-lins,rgba(12,26,36,.08));border-radius:6px;padding:4px 8px;color:var(--helios-mut,#56636E);}
  #hb-{{ sid }} .hb-eta{display:flex;align-items:center;justify-content:center;gap:7px;margin-top:11px;font-size:12.5px;font-weight:700;color:#137a3e;}
  #hb-{{ sid }} .hb-stock{margin-top:14px;background:var(--helios-g-bg,rgba(255,255,255,.55));border:var(--helios-g-bd2,1px solid rgba(255,255,255,.45));border-radius:13px;padding:12px 14px;font-size:12.5px;color:var(--helios-accd,#0A5E97);font-weight:600;}
  #hb-{{ sid }} .hb-stockbar{height:6px;background:#CFE7F5;border-radius:6px;margin-top:9px;overflow:hidden;}
  #hb-{{ sid }} .hb-stockbar i{display:block;height:100%;width:78%;background:linear-gradient(90deg,var(--helios-acc,#0E7CC4),var(--helios-teal,#2FC1D6));border-radius:6px;}
  #hb-{{ sid }} .hb-live{display:flex;align-items:center;gap:8px;justify-content:center;margin-top:12px;font-size:12px;color:var(--helios-mut,#56636E);}
  #hb-{{ sid }} .hb-dot{width:8px;height:8px;border-radius:50%;background:#1aa86b;animation:helios-pulse 1.6s ease-in-out infinite;}
  #hb-toast-{{ sid }}{position:fixed;left:16px;bottom:18px;z-index:120;display:flex;align-items:center;gap:11px;background:rgba(255,255,255,.86);backdrop-filter:var(--helios-g-blur);border:var(--helios-g-bd,1px solid rgba(255,255,255,.65));box-shadow:var(--helios-g-sh);border-radius:14px;padding:11px 15px 11px 12px;max-width:300px;opacity:0;pointer-events:none;transition:opacity .3s;}
  #hb-toast-{{ sid }}.show{opacity:1;animation:helios-toastin .4s ease;}
  #hb-toast-{{ sid }} .hb-tt-em{width:34px;height:34px;border-radius:10px;background:linear-gradient(135deg,var(--helios-acc,#0E7CC4),var(--helios-acc2,#16A6D6));display:grid;place-items:center;font-size:16px;flex-shrink:0;}
  {%- comment -%} build-your-own bundle {%- endcomment -%}
  #hb-{{ sid }} .hb-byo{margin:0 0 12px;border:1px dashed rgba(20,26,32,.24);border-radius:14px;overflow:hidden;background:#fbfcfd;}
  #hb-{{ sid }} .hb-byo-toggle{width:100%;display:flex;align-items:center;justify-content:space-between;gap:10px;background:none;border:none;padding:12px 14px;cursor:pointer;text-align:left;}
  #hb-{{ sid }} .hb-byo-tt b{display:block;font-family:'Archivo',sans-serif;font-weight:800;font-size:14px;color:#0A0D11;}
  #hb-{{ sid }} .hb-byo-tt{flex:1;min-width:0;}
  #hb-{{ sid }} .hb-byo-tt em{display:block;font-style:normal;font-size:11.5px;color:#6b7480;margin-top:1px;}
  #hb-{{ sid }} .hb-byo-tt em strong{color:#15803d;font-weight:800;}
  #hb-{{ sid }} .hb-byo-badge{flex:0 0 auto;font-family:'Archivo',sans-serif;font-weight:800;font-size:10px;letter-spacing:.03em;color:#fff;background:linear-gradient(180deg,#1e9f5c,#127a44);padding:5px 9px;border-radius:20px;white-space:nowrap;box-shadow:0 3px 8px rgba(18,122,68,.3);}
  #hb-{{ sid }} .hb-byo-banner{margin:2px 0 8px;padding:9px 11px;background:#eafaf0;border:1px solid rgba(22,163,74,.25);border-radius:10px;font-size:12px;color:#15803d;line-height:1.35;}
  #hb-{{ sid }} .hb-byo-banner b{font-weight:800;}
  #hb-{{ sid }} .hb-byo-price{font-weight:600;}
  #hb-{{ sid }} .hb-byo-sum{display:flex;align-items:center;flex-wrap:wrap;gap:7px;margin-top:11px;font-family:'Archivo',sans-serif;font-weight:800;font-size:14px;color:#0A0D11;}
  #hb-{{ sid }} .hb-byo-sum s{color:#9aa2ab;font-weight:600;}
  #hb-{{ sid }} .hb-byo-sum b{color:#E11D2A;}
  #hb-{{ sid }} .hb-byo-savepill{font-size:10px;font-weight:800;color:#15803d;background:#daf3e3;padding:4px 8px;border-radius:20px;letter-spacing:.02em;}
  #hb-{{ sid }} .hb-byo-chev{color:#6b7480;flex:0 0 auto;transition:transform .2s;}
  #hb-{{ sid }} .hb-byo.open .hb-byo-chev{transform:rotate(180deg);}
  #hb-{{ sid }} .hb-byo-body{padding:0 12px 12px;}
  #hb-{{ sid }} .hb-byo-row{display:flex;align-items:center;gap:11px;padding:10px 0;border-top:1px solid rgba(20,26,32,.08);}
  #hb-{{ sid }} .hb-byo-img{width:44px;height:44px;border-radius:10px;object-fit:cover;flex:0 0 auto;background:#eef4f9;}
  #hb-{{ sid }} .hb-byo-info{flex:1;min-width:0;}
  #hb-{{ sid }} .hb-byo-info b{display:block;font-family:'Archivo',sans-serif;font-weight:800;font-size:14px;color:#0A0D11;}
  #hb-{{ sid }} .hb-byo-info span{font-size:12.5px;color:#6b7480;}
  #hb-{{ sid }} .hb-byo-step{display:inline-flex;align-items:center;border:1px solid rgba(20,26,32,.16);border-radius:30px;overflow:hidden;flex:0 0 auto;background:#fff;}
  #hb-{{ sid }} .hb-byo-step button{width:32px;height:32px;border:none;background:none;font-size:18px;font-weight:700;color:#0A0D11;cursor:pointer;line-height:1;}
  #hb-{{ sid }} .hb-byo-step button:active{background:#f0f2f4;}
  #hb-{{ sid }} .hb-byo-step span{min-width:24px;text-align:center;font-family:'Archivo',sans-serif;font-weight:800;font-size:14px;color:#0A0D11;}
  #hb-{{ sid }} .hb-byo-add{width:100%;margin-top:11px;background:#0A1219;color:#fff;border:none;border-radius:13px;padding:13px;font-family:'Archivo',sans-serif;font-weight:800;font-size:15px;cursor:pointer;transition:opacity .15s;}
  #hb-{{ sid }} .hb-byo-add:disabled{opacity:.4;cursor:not-allowed;}
  {%- comment -%} ===== MOBILE-FIRST (≤430px): tighter cards, one-line pills, balanced hierarchy ===== {%- endcomment -%}
  @media(max-width:430px){
    #hb-{{ sid }} .hb-top{gap:9px;margin:2px 0 11px;}
    #hb-{{ sid }} .hb-price{gap:8px;}
    #hb-{{ sid }} .hb-price b{font-size:32px;}
    #hb-{{ sid }} .hb-price s{font-size:16px;}
    #hb-{{ sid }} .hb-bundlepill{font-size:9px;padding:6px 9px;margin-top:8px;gap:5px;}
    #hb-{{ sid }} .hb-autodisc{margin-top:8px;gap:7px;}
    #hb-{{ sid }} .hb-autodisc-ck{width:18px;height:18px;}
    #hb-{{ sid }} .hb-autodisc b{font-size:12.5px;}
    #hb-{{ sid }} .hb-autodisc span{font-size:11px;}
    #hb-{{ sid }} .hb-top-right{gap:5px;padding-top:2px;}
    #hb-{{ sid }} .hb-savebox{font-size:16px;padding:10px 13px;border-radius:12px;}
    #hb-{{ sid }} .hb-savesub{font-size:11.5px;}
    #hb-{{ sid }} .hb-shipbox{gap:4px 12px;padding:8px 11px;margin-bottom:11px;}
    #hb-{{ sid }} .hb-shipcol{gap:5px;font-size:11px;}
    #hb-{{ sid }} .hb-shipcol svg{width:15px;height:15px;}
    #hb-{{ sid }} .hb-shipcol b{font-size:11.5px;}
    #hb-{{ sid }} .hb-shipcol i{font-size:11px;}
    #hb-{{ sid }} .hb-tiers{padding:4px;}
    #hb-{{ sid }} .hb-tier{padding:10px 10px;gap:10px;}
    #hb-{{ sid }} .hb-tiername{font-size:15px;}
    #hb-{{ sid }} .hb-tiersub{font-size:12px;}
    #hb-{{ sid }} .hb-tierper{font-size:10.5px;margin-top:1px;}
    #hb-{{ sid }} .hb-tierpill{font-size:8px;padding:4px 7px;gap:3px;margin-top:6px;letter-spacing:.01em;}
    #hb-{{ sid }} .hb-tierpill svg{width:9px;height:9px;}
    #hb-{{ sid }} .hb-tierprice b{font-size:17px;}
    #hb-{{ sid }} .hb-tierprice s{font-size:12px;}
    #hb-{{ sid }} .hb-tiersave{font-size:11px;}
    #hb-{{ sid }} .hb-badge{font-size:9px;padding:4px 10px;top:-10px;left:14px;}
    #hb-{{ sid }} .hb-radio{width:20px;height:20px;}
    #hb-{{ sid }} .hb-tier.sel .hb-radio{border-width:6px;}
  }
</style>

<div class="hb-wrap" id="buy" style="scroll-margin-top:84px;">
  <div class="hb-grid">

    {%- comment -%} ---------- GALLERY (double) ---------- {%- endcomment -%}
    <div class="hb-gallery" data-gal="double">
      <div class="hb-stage" id="hb-stage-{{ sid }}" data-track>
        {%- assign mainimg = product.featured_image | default: section.settings.lead_image -%}
        {%- assign nimg = product.images.size -%}
        {%- capture hb_vidcell -%}<div class="hb-cell" data-slide="vid"><video muted loop playsinline preload="none"{% if section.settings.lead_video.preview_image != blank %} poster="{{ section.settings.lead_video.preview_image | image_url: width: 700 }}"{% endif %}>{%- for s in section.settings.lead_video.sources -%}<source src="{{ s.url }}" type="{{ s.mime_type }}">{%- endfor -%}</video></div>{%- endcapture -%}
        {%- if nimg > 0 -%}
          {%- for img in product.images -%}
            <div class="hb-cell" data-slide="{{ forloop.index0 }}"><img src="{{ img | image_url: width: 700 }}" srcset="{{ img | image_url: width: 400 }} 400w, {{ img | image_url: width: 700 }} 700w, {{ img | image_url: width: 1000 }} 1000w" sizes="(min-width:880px) 540px, 92vw" width="1000" height="1000" alt="{{ product.title | escape }}" loading="{% if forloop.first %}eager{% else %}lazy{% endif %}" decoding="async"{% if forloop.first %} fetchpriority="high"{% endif %}></div>
            {%- if forloop.first and section.settings.lead_video != blank -%}{{ hb_vidcell }}{%- endif -%}
          {%- endfor -%}
        {%- elsif mainimg != blank -%}
          <div class="hb-cell" data-slide="0"><img src="{{ mainimg | image_url: width: 700 }}" srcset="{{ mainimg | image_url: width: 400 }} 400w, {{ mainimg | image_url: width: 700 }} 700w, {{ mainimg | image_url: width: 1000 }} 1000w" sizes="(min-width:880px) 540px, 92vw" width="1000" height="1000" alt="{{ product.title | escape }}" loading="eager" fetchpriority="high"></div>
          {%- if section.settings.lead_video != blank -%}{{ hb_vidcell }}{%- endif -%}
        {%- elsif section.settings.lead_video != blank -%}
          {{ hb_vidcell }}
        {%- endif -%}
        {%- if nimg == 0 and mainimg == blank and section.settings.lead_video == blank -%}
          <div class="hb-cell" data-slide="0"><div style="display:flex;flex-direction:column;align-items:center;justify-content:center;gap:12px;text-align:center;color:var(--helios-mut,#56636E);padding:24px;"><span style="width:58px;height:58px;border-radius:50%;background:rgba(14,124,196,.10);display:grid;place-items:center;color:var(--helios-accd,#0A5E97);"><svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="16" rx="2.5"/><circle cx="8.5" cy="9.5" r="1.6"/><path d="m21 16-4.5-4.5L7 21"/></svg></span><div style="font-family:'Archivo',sans-serif;font-weight:700;font-size:14px;color:var(--helios-ink,#0C1A24);">Add your demo video or photos</div><div style="font-size:12px;opacity:.75;">Product → Media · the first one shows here</div></div></div>
        {%- endif -%}
      </div>
      <div class="hb-dots" data-dots></div>
      <div class="hb-thumbs">
        {%- comment -%} product images first (Product → Media), min 4 slots {%- endcomment -%}
        {%- assign nimg = product.images.size -%}
        {%- if nimg < 4 -%}{%- assign slotcount = 4 -%}{%- else -%}{%- assign slotcount = nimg -%}{%- endif -%}
        {%- for n in (1..slotcount) -%}
          {%- assign idx = n | minus: 1 -%}
          {%- assign img = product.images[idx] -%}
          {%- if img != blank -%}
            <button type="button" class="hb-thumb{% if forloop.first %} sel{% endif %}" data-goto="{{ idx }}"><img src="{{ img | image_url: width: 220 }}" alt="{{ product.title | escape }}" loading="lazy"></button>
          {%- else -%}
            <button type="button" class="hb-thumb" aria-hidden="true"><span style="display:flex;flex-direction:column;align-items:center;justify-content:center;width:100%;height:100%;color:var(--helios-mut,#56636E);font-size:11px;gap:3px;opacity:.7;"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="m21 15-5-5L5 21"/></svg>img</span></button>
          {%- endif -%}
          {%- comment -%} video as the SECOND thumbnail — autoplays {%- endcomment -%}
          {%- if forloop.first and section.settings.lead_video != blank -%}
          <button type="button" class="hb-thumb hb-thumb-vid" data-goto="vid">
            <video muted loop playsinline autoplay preload="auto"{% if section.settings.lead_video.preview_image != blank %} poster="{{ section.settings.lead_video.preview_image | image_url: width: 220 }}"{% endif %}>{%- for s in section.settings.lead_video.sources -%}<source src="{{ s.url }}" type="{{ s.mime_type }}">{%- endfor -%}</video>
          </button>
          {%- endif -%}
        {%- endfor -%}
      </div>
    </div>

    {%- comment -%} ---------- GALLERY (mini) ---------- {%- endcomment -%}
    <div class="hb-gallery" data-gal="mini" style="display:none;">
      <div class="hb-stage" data-track>
        {%- assign mhas = false -%}
        {%- for i in (1..4) -%}
          {%- case i -%}{%- when 1 -%}{%- assign mi = section.settings.mini_image_1 -%}{%- when 2 -%}{%- assign mi = section.settings.mini_image_2 -%}{%- when 3 -%}{%- assign mi = section.settings.mini_image_3 -%}{%- when 4 -%}{%- assign mi = section.settings.mini_image_4 -%}{%- endcase -%}
          {%- if mi != blank -%}<div class="hb-cell" data-slide="{{ i }}"><img src="{{ mi | image_url: width: 700 }}" srcset="{{ mi | image_url: width: 400 }} 400w, {{ mi | image_url: width: 700 }} 700w, {{ mi | image_url: width: 1000 }} 1000w" sizes="(min-width:880px) 540px, 92vw" width="1000" height="1000" alt="Super Glide Mini" loading="{% unless mhas %}eager{% else %}lazy{% endunless %}" decoding="async"></div>{%- assign mhas = true -%}{%- endif -%}
        {%- endfor -%}
        {%- if section.settings.mini_video_1 != blank -%}<div class="hb-cell" data-slide="v1"><video muted loop playsinline preload="none"{% if section.settings.mini_video_1.preview_image != blank %} poster="{{ section.settings.mini_video_1.preview_image | image_url: width: 700 }}"{% endif %}>{%- for s in section.settings.mini_video_1.sources -%}<source src="{{ s.url }}" type="{{ s.mime_type }}">{%- endfor -%}</video></div>{%- assign mhas = true -%}{%- endif -%}
        {%- if section.settings.mini_video_2 != blank -%}<div class="hb-cell" data-slide="v2"><video muted loop playsinline preload="none"{% if section.settings.mini_video_2.preview_image != blank %} poster="{{ section.settings.mini_video_2.preview_image | image_url: width: 700 }}"{% endif %}>{%- for s in section.settings.mini_video_2.sources -%}<source src="{{ s.url }}" type="{{ s.mime_type }}">{%- endfor -%}</video></div>{%- endif -%}
        {%- unless mhas -%}<div class="hb-cell" data-slide="1"><div style="display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;color:var(--helios-mut,#56636E);padding:24px;text-align:center;"><span style="width:52px;height:52px;border-radius:50%;background:rgba(14,124,196,.10);display:grid;place-items:center;color:var(--helios-accd,#0A5E97);">🌀</span><div style="font-family:'Archivo',sans-serif;font-weight:700;font-size:14px;color:var(--helios-ink,#0C1A24);">Add Mini media</div><div style="font-size:12px;opacity:.75;">Customize → Mini media (2 videos + images)</div></div></div>{%- endunless -%}
      </div>
      <div class="hb-dots" data-dots></div>
      <div class="hb-thumbs">
        {%- for i in (1..4) -%}
          {%- case i -%}{%- when 1 -%}{%- assign mi = section.settings.mini_image_1 -%}{%- when 2 -%}{%- assign mi = section.settings.mini_image_2 -%}{%- when 3 -%}{%- assign mi = section.settings.mini_image_3 -%}{%- when 4 -%}{%- assign mi = section.settings.mini_image_4 -%}{%- endcase -%}
          {%- if mi != blank -%}
            <button type="button" class="hb-thumb{% if forloop.first %} sel{% endif %}" data-goto="{{ i }}"><img src="{{ mi | image_url: width: 220 }}" alt="Super Glide Mini" loading="lazy"></button>
          {%- else -%}
            <button type="button" class="hb-thumb" aria-hidden="true"><span style="display:flex;flex-direction:column;align-items:center;justify-content:center;width:100%;height:100%;color:var(--helios-mut,#56636E);font-size:11px;gap:3px;opacity:.7;"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="m21 15-5-5L5 21"/></svg>img</span></button>
          {%- endif -%}
        {%- endfor -%}
        {%- if section.settings.mini_video_1 != blank -%}<button type="button" class="hb-thumb" data-goto="v1">{%- if section.settings.mini_video_1.preview_image != blank -%}<img src="{{ section.settings.mini_video_1.preview_image | image_url: width: 220 }}" alt="video"><span style="position:absolute;inset:0;display:grid;place-items:center;color:#fff;font-size:18px;text-shadow:0 1px 6px rgba(0,0,0,.6);">▶</span>{%- else -%}<span style="display:flex;flex-direction:column;align-items:center;justify-content:center;width:100%;height:100%;color:var(--helios-accd,#0A5E97);font-size:11px;gap:3px;">▶<span style="opacity:.7;">video</span></span>{%- endif -%}</button>{%- endif -%}
        {%- if section.settings.mini_video_2 != blank -%}<button type="button" class="hb-thumb" data-goto="v2">{%- if section.settings.mini_video_2.preview_image != blank -%}<img src="{{ section.settings.mini_video_2.preview_image | image_url: width: 220 }}" alt="video 2"><span style="position:absolute;inset:0;display:grid;place-items:center;color:#fff;font-size:18px;text-shadow:0 1px 6px rgba(0,0,0,.6);">▶</span>{%- else -%}<span style="display:flex;flex-direction:column;align-items:center;justify-content:center;width:100%;height:100%;color:var(--helios-accd,#0A5E97);font-size:11px;gap:3px;">▶<span style="opacity:.7;">video 2</span></span>{%- endif -%}</button>{%- endif -%}
      </div>
    </div>

    {%- comment -%} ---------- BUY ---------- {%- endcomment -%}
    <div class="hb-buy">
      {%- comment -%} clean, Glo-style header {%- endcomment -%}
      <div class="hb-eyebrow">{{ section.settings.eyebrow | default: 'The motorized water lounger' }}</div>
      <h1 id="hb-title-{{ sid }}" data-double="{{ section.settings.title | default: 'HELIOS Super Glide' }}" data-mini="HELIOS Mini Glide">{{ section.settings.title | default: 'HELIOS Super Glide' }}</h1>
      <p class="hb-sub">{{ section.settings.subtitle | default: 'Kick back, grab the joysticks, and glide across the water. No paddling — just cruise.' }}</p>

      <div class="hb-pills">
        <span class="hb-pill">{{ section.settings.pill_1 | default: 'Hands-free thrusters' }}</span>
        <span class="hb-pill">{{ section.settings.pill_2 | default: 'Fits the whole family' }}</span>
        <span class="hb-pill">{{ section.settings.pill_3 | default: 'Inflates in ~5 min' }}</span>
      </div>

      <a href="#reviews" class="hb-rate">
        <span style="color:#f5a623;letter-spacing:1px;">★★★★★</span>
        <b style="color:var(--helios-ink,#0C1A24);">{{ section.settings.rating | default: '4.8' }}</b>
        <span class="hb-metadot">·</span><span class="hb-metaspec">{{ section.settings.review_count | default: '3,912' }} reviews</span>
        <span class="hb-metadot">·</span><span class="hb-metaspec">{{ section.settings.meta_1 | default: 'dual thrusters' }}</span>
        <span class="hb-metadot">·</span><span class="hb-metaspec">{{ section.settings.meta_2 | default: 'holds 300 lb' }}</span>
      </a>

      <div class="hb-priceline">
        <b class="hb-price-main" id="hb-price-{{ sid }}">{{ cur.price | times: 0.88 | round | money }}</b>
        <s id="hb-cmp-{{ sid }}"{% unless cur.compare_at_price > cur.price %} style="display:none;"{% endunless %}>{{ cur.compare_at_price | money }}</s>
        <span class="hb-savesub" id="hb-savesub-{{ sid }}"></span>
      </div>

      {%- comment -%} ========== REAL SHOPIFY FORM ========== {%- endcomment -%}
      {%- assign hb_form_id = 'hb-form-' | append: sid -%}
      {%- form 'product', product, id: hb_form_id -%}
        <input type="hidden" name="id" id="hb-vid-{{ sid }}" value="{{ cur.id }}">

        {%- if section.settings.show_picker -%}
        {%- comment -%} variant picker (Super Glide / Mini) — right above the bundles {%- endcomment -%}
        <div class="hb-picker" style="display:flex;gap:9px;margin:0 0 14px;">
          {%- assign pk_dbl = product.featured_image | default: section.settings.lead_image -%}
          <button type="button" data-pick="double" class="hb-pk on">
            {%- if pk_dbl != blank -%}<img class="hb-pkimg" src="{{ pk_dbl | image_url: width: 120 }}" alt="Super Glide">{%- endif -%}
            <span class="hb-pktx"><b>Super Glide</b><span>Double · 2 seats</span></span>
          </button>
          {%- assign mini_variant = blank -%}
          {%- for v in product.variants -%}{%- if v.title contains 'Mini' -%}{%- assign mini_variant = v -%}{%- break -%}{%- endif -%}{%- endfor -%}
          {%- assign pk_mini = mini_variant.image | default: section.settings.mini_image_1 -%}
          <button type="button" data-pick="mini" class="hb-pk">
            {%- if pk_mini != blank -%}<img class="hb-pkimg" src="{{ pk_mini | image_url: width: 120 }}" alt="Mini Glide">{%- endif -%}
            <span class="hb-pktx"><b>Mini Glide</b><span>Single · 1 seat</span></span>
          </button>
        </div>
        {%- endif -%}

        <div class="hb-tiers" id="hb-tiers-{{ sid }}">
          {%- if section.settings.show_picker -%}
          {%- comment -%} auto Mini tier (single-seat variant) {%- endcomment -%}
          {%- for v in product.variants -%}
            {%- if v.title contains 'Mini' -%}
              {%- assign mqty = 1 -%}{%- assign msub = 'Single seat · most affordable' -%}{%- assign mbadge = '' -%}{%- assign msel = '' -%}
              {%- if v.title contains '2' -%}{%- assign mqty = 2 -%}{%- assign msub = 'BUY 1 GET 1 50% OFF · race a friend' -%}{%- assign mbadge = 'Most popular' -%}{%- assign msel = ' sel' -%}{%- endif -%}
              {%- if v.title contains '3' -%}{%- assign mqty = 3 -%}{%- assign msub = 'Best value · the whole crew' -%}{%- assign mbadge = 'Best value' -%}{%- endif -%}
              {%- assign mper = v.price | divided_by: mqty -%}
              <label class="hb-tier{{ msel }}" data-tier="mini" style="display:none;" data-vid="{{ v.id }}" data-price="{{ v.price }}" data-compare="{{ v.compare_at_price }}">
                {%- if mbadge != blank -%}<span class="hb-badge{% if mbadge == 'Best value' %} teal{% endif %}">{{ mbadge }}</span>{%- endif -%}
                <span class="hb-radio"></span>
                <div style="flex:1;">
                  <span class="hb-tiername">{{ mqty }} × Mini Glide</span>
                  <span class="hb-tiersub">{{ msub }}</span>
                  <span class="hb-tierper">{{ mper | times: 0.88 | round | money }} / glider</span>
                </div>
                <div class="hb-tierprice">
                  <b>{{ v.price | times: 0.88 | round | money }}</b>
                  {%- if v.compare_at_price > v.price -%}<s>{{ v.compare_at_price | money }}</s>{%- endif -%}
                </div>
              </label>
            {%- endif -%}
          {%- endfor -%}
          {%- endif -%}
          {%- for block in section.blocks -%}
            {%- if block.type == 'bundle' -%}
              {%- assign vid = block.settings.variant_id | times: 1 -%}
              {%- assign bv = blank -%}
              {%- for v in product.variants -%}{%- if v.id == vid -%}{%- assign bv = v -%}{%- endif -%}{%- endfor -%}
              {%- assign qty = block.settings.qty | default: 1 -%}
              {%- assign tprice = bv.price | default: block.settings.fallback_price | plus: 0 -%}
              {%- assign tcompare = bv.compare_at_price | default: block.settings.fallback_compare | plus: 0 -%}
              {%- assign saveamt = tcompare | minus: tprice -%}
              {%- assign perunit = tprice | divided_by: qty -%}
              {%- assign pilltxt = 'FOR ' | append: block.settings.label | upcase -%}
              {%- if qty == 2 -%}{%- assign pilltxt = pilltxt | append: ' (2-PACK)' -%}{%- endif -%}
              {%- if qty == 4 -%}{%- assign pilltxt = pilltxt | append: ' (2+2)' -%}{%- endif -%}
              <label class="hb-tier{% if block.settings.preselect %} sel{% endif %}" data-tier="double"
                     data-vid="{{ bv.id | default: vid }}" data-price="{{ tprice }}" data-compare="{{ tcompare }}" data-pill="{{ pilltxt }}"
                     {{ block.shopify_attributes }}>
                {%- if block.settings.badge != blank -%}
                  <span class="hb-badge{% if block.settings.badge_style == 'teal' %} teal{% endif %}">{{ block.settings.badge }}</span>
                {%- endif -%}
                <span class="hb-radio"></span>
                <div style="flex:1;">
                  <span class="hb-tiername">{{ block.settings.label | default: bv.title }}</span>
                  {%- if block.settings.sublabel != blank -%}<span class="hb-tiersub">{{ block.settings.sublabel }}</span>{%- endif -%}
                  <span class="hb-tierper">{{ perunit | times: 0.88 | round | money }} each</span>
                </div>
                <div class="hb-tierprice">
                  <b>{{ tprice | times: 0.88 | round | money }}</b>
                  {%- if tcompare > tprice -%}<s>{{ tcompare | money }}</s>{%- endif -%}
                </div>
              </label>
            {%- endif -%}
          {%- endfor -%}
        </div>

        <button type="submit" name="add" class="hb-atc" {% unless cur.available %}disabled{% endunless %}>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/></svg>
          {{ section.settings.cta_text | default: 'Add to Cart' }}
        </button>
        <button type="button" class="hb-buynow" id="hb-buynow-{{ sid }}" {% unless cur.available %}disabled{% endunless %}>Buy now</button>
      {%- endform -%}


      {%- if section.settings.discount_code != blank -%}
        <button type="button" class="hb-code" id="hb-code-{{ sid }}" data-code="{{ section.settings.discount_code }}">
          <span style="font-size:12.5px;color:var(--helios-mut,#56636E);">{{ section.settings.discount_label | default: 'Extra savings — tap to copy' }}</span>
          <b>{{ section.settings.discount_code }}
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="11" height="11" rx="2"></rect><path d="M5 15V5a2 2 0 0 1 2-2h10"></path></svg>
          </b>
        </button>
      {%- endif -%}

      <div class="hb-trust"><span>⚡ One-tap checkout</span><span>🚚 Free shipping</span><span>🛡 30-day refund</span><span>📦 Ships together</span></div>
      <div class="hb-pays"><span>VISA</span><span>MASTERCARD</span><span>AMEX</span><span>PAYPAL</span><span>APPLE PAY</span><span>SHOP PAY</span></div>

      {%- if section.settings.eta_line != blank -%}<div class="hb-eta">🚚 {{ section.settings.eta_line }}</div>{%- endif -%}

      {%- if section.settings.show_stock -%}
        <div class="hb-stock">🔥 Summer demand is high — only <b>{{ section.settings.stock_left | default: 24 }}</b> left at this price.
          <div class="hb-stockbar"><i></i></div>
        </div>
      {%- endif -%}

      {%- if section.settings.show_live -%}
        <div class="hb-live"><span class="hb-dot"></span>
          <b id="hb-view-{{ sid }}" style="color:var(--helios-ink,#0C1A24);">{{ section.settings.live_viewers | default: 58 }}</b> viewing ·
          <b id="hb-sold-{{ sid }}" style="color:var(--helios-ink,#0C1A24);">{{ section.settings.sold_today | default: 202 }}</b> sold today
        </div>
      {%- endif -%}
    </div>
  </div>
</div>

<div id="hb-sticky-{{ sid }}" aria-hidden="true">
  <div class="hb-sk-info">
    {%- if product.featured_image != blank -%}<img class="hb-sk-thumb" src="{{ product.featured_image | image_url: width: 120 }}" alt="">{%- endif -%}
    <div style="min-width:0;">
      <div class="hb-sk-name">{{ section.settings.title | default: 'HELIOS Super Glide' }}</div>
      <div class="hb-sk-price"><b id="hb-sk-price-{{ sid }}">{{ cur.price | times: 0.88 | round | money }}</b></div>
    </div>
  </div>
  <button type="button" class="hb-sk-add" id="hb-sk-add-{{ sid }}">Add</button>
</div>

{%- if section.settings.show_toasts -%}
  <div id="hb-toast-{{ sid }}"><span class="hb-tt-em">🌊</span><div style="line-height:1.25;"><div id="hb-toast-name-{{ sid }}" style="font-size:13px;font-weight:700;color:var(--helios-ink,#0C1A24);"></div><div id="hb-toast-meta-{{ sid }}" style="font-size:11.5px;color:var(--helios-mut,#56636E);"></div></div></div>
{%- endif -%}

<script>
(function(){
  var root = document.getElementById('hb-{{ sid }}');
  if(!root || root.dataset.init) return; root.dataset.init = '1';

  (function(){
    var sk = document.getElementById('hb-sticky-{{ sid }}');
    var atc = root.querySelector('.hb-atc');
    if(!sk || !atc) return;
    if(sk.parentNode !== document.body) document.body.appendChild(sk);
    if('IntersectionObserver' in window){
      new IntersectionObserver(function(ents){ ents.forEach(function(en){ sk.classList.toggle('show', !en.isIntersecting); }); }, {threshold:0}).observe(atc);
    }
    var skAdd = document.getElementById('hb-sk-add-{{ sid }}');
    if(skAdd) skAdd.addEventListener('click', function(){ atc.click(); });
    var skPrice = document.getElementById('hb-sk-price-{{ sid }}');
    window.addEventListener('helios:variant', function(e){ if(e.detail && e.detail.price && skPrice) skPrice.textContent = e.detail.price; });
  })();
  var money = function(cents){ return '{{ shop.money_format | strip_html }}'.replace(/\{\{\s*amount\s*\}\}/, (cents/100).toFixed(2)).replace(/\{\{\s*amount_no_decimals\s*\}\}/, Math.round(cents/100)); };


  /* ---- bundle tiers → hidden variant id + total + savings ---- */
  var hid = document.getElementById('hb-vid-{{ sid }}');
  var total = document.getElementById('hb-total-{{ sid }}');
  var price = document.getElementById('hb-price-{{ sid }}');
  var cmp = document.getElementById('hb-cmp-{{ sid }}');
  var savebox = document.getElementById('hb-savebox-{{ sid }}');
  var savesub = document.getElementById('hb-savesub-{{ sid }}');
  var bundlepill = document.getElementById('hb-bundlepill-{{ sid }}');
  var tiers = root.querySelectorAll('.hb-tier');
  function selectTier(t){
    tiers.forEach(function(x){ x.classList.remove('sel'); });
    t.classList.add('sel');
    var p = Math.round(parseInt(t.dataset.price,10)*0.88), c = parseInt(t.dataset.compare,10) || 0;
    if(hid) hid.value = t.dataset.vid;
    if(total) total.textContent = money(p);
    if(price) price.textContent = money(p);
    if(cmp){ if(c>p){ cmp.style.display=''; cmp.textContent = money(c); } else cmp.style.display='none'; }
    var pct = c>p ? Math.round((1-p/c)*100) : 0;
    if(savebox){ if(pct>0){ savebox.style.display=''; savebox.textContent = 'SAVE ' + pct + '%'; } else savebox.style.display='none'; }
    if(savesub){ if(c>p){ savesub.style.display=''; savesub.textContent = 'You save ' + money(c-p); } else savesub.style.display='none'; }
    if(bundlepill && t.dataset.pill){ bundlepill.textContent = '📦 ' + t.dataset.pill; }
    try{ var u=new URL(window.location); u.searchParams.set('variant', t.dataset.vid); history.replaceState({}, '', u); }catch(e){}
    window.dispatchEvent(new CustomEvent('helios:variant', { detail: { id: t.dataset.vid, price: money(p), compare: c>p ? money(c) : '' } }));
  }
  tiers.forEach(function(t){ t.addEventListener('click', function(e){ e.preventDefault(); selectTier(t); }); });

  /* ---- buy now → add selected bundle, straight to checkout ---- */
  var DISC = '{{ section.settings.discount_code }}';
  var buynow = document.getElementById('hb-buynow-{{ sid }}');
  if(buynow) buynow.addEventListener('click', function(){
    var vid = (hid && hid.value) ? hid.value : '{{ cur.id }}';
    var label = buynow.textContent; buynow.disabled = true; buynow.textContent = '…';
    fetch('/cart/add.js', { method:'POST', headers:{'Content-Type':'application/json','Accept':'application/json'}, body: JSON.stringify({ id: parseInt(vid,10), quantity: 1 }) })
      .then(function(){ window.location.href = DISC ? ('/discount/'+DISC+'?redirect=%2Fcheckout') : '/checkout'; })
      .catch(function(){ buynow.disabled = false; buynow.textContent = label; });
  });

  /* ---- displayed price = paid price: route the native add-to-cart through the code too ---- */
  var hbf = document.querySelector('#hb-{{ sid }} form[action*="/cart/add"]');
  if(hbf && DISC){ hbf.addEventListener('submit', function(e){
    e.preventDefault();
    fetch('/cart/add.js', { method:'POST', body: new FormData(hbf) })
      .then(function(r){ if(!r.ok) throw 0; window.location.href = '/discount/'+DISC+'?redirect=%2Fcart'; })
      .catch(function(){ hbf.submit(); });
  }); }

  /* ---- gallery thumbs (generic for double + mini) ---- */
  function bindGallery(gal){
    var track = gal.querySelector('[data-track]'); if(!track) return;
    var cells = Array.prototype.slice.call(track.querySelectorAll('.hb-cell'));
    var thumbs = Array.prototype.slice.call(gal.querySelectorAll('.hb-thumb[data-goto]'));
    var dotsWrap = gal.querySelector('[data-dots]');
    if(!cells.length) return;
    if(dotsWrap){ dotsWrap.innerHTML=''; cells.forEach(function(c,i){ var d=document.createElement('span'); d.className='hb-dot'+(i===0?' on':''); dotsWrap.appendChild(d); }); }
    var dots = dotsWrap ? Array.prototype.slice.call(dotsWrap.children) : [];
    function setActive(i){
      dots.forEach(function(d,j){ d.classList.toggle('on', j===i); });
      var key = cells[i].getAttribute('data-slide');
      thumbs.forEach(function(t){ t.classList.toggle('sel', t.getAttribute('data-goto')===key); });
      cells.forEach(function(c,j){ var v=c.querySelector('video'); if(v){ if(j===i){ var p=v.play(); if(p&&p.catch)p.catch(function(){}); } else { try{ v.pause(); }catch(e){} } } });
    }
    if('IntersectionObserver' in window){
      var io=new IntersectionObserver(function(ents){
        ents.forEach(function(en){ if(en.isIntersecting && en.intersectionRatio>=0.55){ var i=cells.indexOf(en.target); if(i>=0) setActive(i); } });
      },{root:track,threshold:[0.55,0.8]});
      cells.forEach(function(c){ io.observe(c); });
    }
    thumbs.forEach(function(t){
      t.addEventListener('click', function(){
        var key=t.getAttribute('data-goto');
        for(var i=0;i<cells.length;i++){ if(cells[i].getAttribute('data-slide')===key){ track.scrollTo({left:cells[i].offsetLeft,behavior:'smooth'}); setActive(i); break; } }
      });
    });
  }
  root.querySelectorAll('[data-gal]').forEach(bindGallery);

  /* ---- variant picker (Double / Mini) + Meta ad deep-link ---- */
  var picks = root.querySelectorAll('[data-pick]');
  function setMode(m){
    picks.forEach(function(p){ p.classList.toggle('on', p.dataset.pick===m); });
    root.querySelectorAll('[data-gal]').forEach(function(g){ g.style.display=g.dataset.gal===m?'':'none'; });
    root.querySelectorAll('[data-tier]').forEach(function(t){ t.style.display=t.dataset.tier===m?'':'none'; });
    var ttl=document.getElementById('hb-title-{{ sid }}'); if(ttl){ var nm=(m==='mini')?ttl.getAttribute('data-mini'):ttl.getAttribute('data-double'); if(nm) ttl.textContent=nm; }
    var def = root.querySelector('[data-tier="'+m+'"].sel') || root.querySelector('[data-tier="'+m+'"]');
    if(def) selectTier(def);
  }
  picks.forEach(function(p){ p.addEventListener('click', function(){ setMode(p.dataset.pick); }); });
  (function(){
    var qv=null; try{ qv=new URL(window.location).searchParams.get('variant'); }catch(e){}
    var st = qv ? root.querySelector('.hb-tier[data-vid="'+qv+'"]') : null;
    if(st){ setMode(st.dataset.tier || 'double'); selectTier(st); }
    else { setMode('double'); }
  })();

  /* ---- copy code ---- */
  var code = document.getElementById('hb-code-{{ sid }}');
  if(code) code.addEventListener('click', function(){
    var c = code.dataset.code, b = code.querySelector('b');
    var done = function(){ var html=b.innerHTML; b.textContent='✓ Copied '+c+'!'; code.style.borderColor='#1aa86b'; setTimeout(function(){ b.innerHTML=html; code.style.borderColor=''; },1800); };
    if(navigator.clipboard){ navigator.clipboard.writeText(c).then(done).catch(done); } else done();
  });


  /* ---- honest per-session countdown ---- */
  {%- if section.settings.show_countdown -%}
  (function(){
    var el = document.getElementById('hb-cd-{{ sid }}'); if(!el) return;
    var span = {{ section.settings.countdown_minutes | default: 480 }} * 60000;
    var key = 'helios_cd_{{ sid }}';
    var end = parseInt(sessionStorage.getItem(key),10);
    if(!end || isNaN(end) || end < Date.now()){ end = Date.now() + span; sessionStorage.setItem(key, end); }
    function tick(){ var d=Math.max(0,end-Date.now()); var h=Math.floor(d/3.6e6),m=Math.floor(d%3.6e6/6e4),s=Math.floor(d%6e4/1e3); el.textContent = (h<10?'0':'')+h+':'+(m<10?'0':'')+m+':'+(s<10?'0':'')+s; }
    tick(); setInterval(tick,1000);
  })();
  {%- endif -%}

  /* ---- live viewers ---- */
  {%- if section.settings.show_live -%}
  (function(){
    var lv=document.getElementById('hb-view-{{ sid }}'), sd=document.getElementById('hb-sold-{{ sid }}');
    var v={{ section.settings.live_viewers | default: 58 }}, s={{ section.settings.sold_today | default: 202 }};
    setInterval(function(){ v=Math.max(20,v+(Math.random()<0.5?-1:1)*(1+Math.floor(Math.random()*3))); if(lv)lv.textContent=v; if(Math.random()<0.4){ s++; if(sd)sd.textContent=s; } },3200);
  })();
  {%- endif -%}

  /* ---- recent-purchase toasts ---- */
  {%- if section.settings.show_toasts -%}
  (function(){
    var box=document.getElementById('hb-toast-{{ sid }}'),
        nm=document.getElementById('hb-toast-name-{{ sid }}'),
        mt=document.getElementById('hb-toast-meta-{{ sid }}');
    if(!box) return;
    var names=[['Jordan from TX','bought 2 — 3 min ago'],['Maya from HI','bought 1 — 7 min ago'],['Chris from NC','bought 4 — 12 min ago'],['Sophie from FL','bought 2 — 18 min ago'],['Devon from AZ','bought 1 — 24 min ago'],['Natalie from SC','bought 2 — 31 min ago']];
    var i=0;
    function show(){ var n=names[i%names.length]; i++; nm.textContent=n[0]; mt.textContent='Super Glide · '+n[1]; box.classList.add('show'); setTimeout(function(){ box.classList.remove('show'); }, 4200); }
    setTimeout(function loop(){ show(); setTimeout(loop, 11000); }, 6000);
  })();
  {%- endif -%}
})();
</script>


<script>
/* HELIOS — remove the app-injected "Product details" card (AfterSell re-adds it; this kills it on sight). */
(function(){
  function kill(){
    var nodes = document.querySelectorAll('h1,h2,h3,h4,h5,strong,p,span,div');
    for(var i=0;i<nodes.length;i++){
      var n = nodes[i];
      if(n.children.length===0 && (n.textContent||'').trim().toLowerCase()==='product details'){
        var el = n;
        for(var up=0; up<8 && el && el.parentElement; up++){
          el = el.parentElement;
          var tx = el.textContent || '';
          if(tx.indexOf('HELIOS Super Glide')>-1 && tx.indexOf('Jet Ski')>-1 && el.offsetHeight>140){
            el.style.setProperty('display','none','important');
            el.setAttribute('data-hb-killed','1');
            return true;
          }
        }
      }
    }
    return false;
  }
  if(!kill()){
    var t=0, iv=setInterval(function(){ if(kill() || ++t>16){ clearInterval(iv); } }, 400);
  }
  document.addEventListener('DOMContentLoaded', kill);
})();
</script>

{%- comment -%} =================== SCHEMA =================== {%- endcomment -%}
{% schema %}
{
  "name": "HELIOS Buy Box",
  "tag": "section",
  "class": "helios-section",
  "settings": [
    { "type": "product", "id": "featured_product", "label": "Product (for homepage / non-product templates)" },
    { "type": "header", "content": "Lead media (A/B: video vs image)" },
    { "type": "select", "id": "lead_media_type", "label": "Lead media", "options": [{"value":"video","label":"Demo video (proof of motion)"},{"value":"image","label":"Image"}], "default": "video" },
    { "type": "video", "id": "lead_video", "label": "Demo video (autoplay muted loop)" },
    { "type": "image_picker", "id": "lead_image", "label": "Lead / poster image" },
    { "type": "header", "content": "Mini Glider media (shows when Mini is picked)" },
    { "type": "video", "id": "mini_video_1", "label": "Mini video 1" },
    { "type": "video", "id": "mini_video_2", "label": "Mini video 2" },
    { "type": "image_picker", "id": "mini_image_1", "label": "Mini image 1" },
    { "type": "image_picker", "id": "mini_image_2", "label": "Mini image 2" },
    { "type": "image_picker", "id": "mini_image_3", "label": "Mini image 3" },
    { "type": "image_picker", "id": "mini_image_4", "label": "Mini image 4" },
    { "type": "header", "content": "Copy (A/B)" },
    { "type": "text", "id": "title", "label": "Product title", "default": "HELIOS Super Glide" },
    { "type": "richtext", "id": "subtitle", "label": "Subtitle" },
    { "type": "text", "id": "rating", "label": "Rating", "default": "4.8" },
    { "type": "text", "id": "review_count", "label": "Review count", "default": "3,912" },
    { "type": "checkbox", "id": "tiktok_badge", "label": "Show 'As seen on TikTok' badge", "default": true },
    { "type": "checkbox", "id": "show_picker", "label": "Show Super/Mini variant picker", "default": true },
    { "type": "text", "id": "cta_text", "label": "Add-to-cart text", "default": "Add to Cart" },
    { "type": "header", "content": "Discount chip (use a REAL active code)" },
    { "type": "text", "id": "discount_code", "label": "Discount code", "default": "SUPERGLIDEIT" },
    { "type": "text", "id": "discount_label", "label": "Discount label", "default": "Extra savings — tap to copy" },
    { "type": "header", "content": "#1 seller / best-price banner (under buy buttons)" },
    { "type": "checkbox", "id": "show_bestprice", "label": "Show '#1 seller · best price' banner", "default": true },
    { "type": "text", "id": "bestprice_pill", "label": "Gold pill text", "default": "#1 Seller in the U.S." },
    { "type": "text", "id": "bestprice_tag", "label": "Secondary tag", "default": "Lowest price, guaranteed" },
    { "type": "text", "id": "bestprice_head", "label": "Headline", "default": "The best price in the country." },
    { "type": "text", "id": "competitor_price", "label": "Competitor price (each)", "default": "$350" },
    { "type": "text", "id": "our_price", "label": "Our 2-pack price", "default": "$299" },
    { "type": "header", "content": "4th of July auto-coupon advert (above buy buttons)" },
    { "type": "checkbox", "id": "show_promo4", "label": "Show auto-coupon advert", "default": true },
    { "type": "text", "id": "promo4_tag", "label": "Tag", "default": "4th of July" },
    { "type": "text", "id": "promo4_amount", "label": "Discount amount", "default": "$21.99" },
    { "type": "number", "id": "promo4_days", "label": "Days remaining", "default": 3 },
    { "type": "header", "content": "Urgency & social proof" },
    { "type": "checkbox", "id": "show_countdown", "label": "Show countdown", "default": true },
    { "type": "range", "id": "countdown_minutes", "label": "Countdown length (minutes, per session)", "min": 30, "max": 1440, "step": 30, "default": 480 },
    { "type": "text", "id": "eta_line", "label": "Delivery ETA line", "default": "Order today → ships in 24h, arrives in 5–8 days" },
    { "type": "checkbox", "id": "show_stock", "label": "Show low-stock bar", "default": true },
    { "type": "number", "id": "stock_left", "label": "Stock left", "default": 24 },
    { "type": "checkbox", "id": "show_live", "label": "Show live viewers / sold", "default": true },
    { "type": "number", "id": "live_viewers", "label": "Viewers (starting)", "default": 58 },
    { "type": "number", "id": "sold_today", "label": "Sold today (starting)", "default": 202 },
    { "type": "checkbox", "id": "show_toasts", "label": "Show recent-purchase toasts", "default": true }
  ],
  "blocks": [
    {
      "type": "bundle",
      "name": "Bundle tier",
      "settings": [
        { "type": "text", "id": "variant_id", "label": "Variant ID (real, numeric)", "info": "Products → variant → number at end of admin URL" },
        { "type": "number", "id": "qty", "label": "Units in this bundle", "default": 1 },
        { "type": "text", "id": "label", "label": "Tier label", "default": "2 Super Glide" },
        { "type": "text", "id": "sublabel", "label": "Sub label", "default": "BUY ONE GET ONE FREE · Race a friend" },
        { "type": "text", "id": "badge", "label": "Badge text (optional)", "default": "Most popular" },
        { "type": "select", "id": "badge_style", "label": "Badge style", "options": [{"value":"accent","label":"Accent gradient"},{"value":"teal","label":"Teal"}], "default": "accent" },
        { "type": "checkbox", "id": "preselect", "label": "Pre-select this tier", "default": false },
        { "type": "text", "id": "fallback_price", "label": "Fallback price (cents)", "info": "Only used if Variant ID is empty", "default": "19999" },
        { "type": "text", "id": "fallback_compare", "label": "Fallback compare-at (cents)", "default": "39998" }
      ]
    },
    {
      "type": "thumb",
      "name": "Gallery thumbnail",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Image" }
      ]
    }
  ],
  "presets": [
    {
      "name": "HELIOS Buy Box",
      "blocks": [
        { "type": "bundle", "settings": { "qty": 2, "label": "1 + 1 · 2 Super Glides", "sublabel": "BUY ONE GET ONE FREE · Race a friend", "badge": "Most popular", "badge_style": "accent", "preselect": true, "fallback_price": "19999", "fallback_compare": "39998" } },
        { "type": "bundle", "settings": { "qty": 3, "label": "3 Super Glides", "sublabel": "Best price per lounger", "badge": "", "badge_style": "accent", "preselect": false, "fallback_price": "33600", "fallback_compare": "59997" } },
        { "type": "bundle", "settings": { "qty": 4, "label": "2 + 2 · 4 Super Glides", "sublabel": "BUY TWO GET TWO FREE · The whole crew", "badge": "Best value", "badge_style": "teal", "preselect": false, "fallback_price": "39999", "fallback_compare": "79996" } },
        { "type": "thumb" }, { "type": "thumb" }, { "type": "thumb" }
      ]
    }
  ]
}
{% endschema %}
</section>
