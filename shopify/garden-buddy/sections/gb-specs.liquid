{%- comment -%}
  GARDEN BUDDY — the spec table.
{%- endcomment -%}
{{ 'gb.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign s = section.settings
  assign fill_open = '<span class="gb-fill">[ '
  assign fill_close = ' ]</span>'
  assign spec_n = 0
  for block in section.blocks
    if block.type == 'spec'
      assign spec_n = spec_n | plus: 1
    endif
  endfor
-%}

<div class="gb gb-pb">

  {%- if spec_n > 0 -%}
  <section class="gb-sec gb-sec--sand gb-detail" id="specs">
    <div class="gb-wrap">
      <div class="gb-specs">
        <div class="gb-head gb-head--tight">
          {%- if s.spec_eyebrow != blank -%}<p class="gb-eyebrow">{{ s.spec_eyebrow }}</p>{%- endif -%}
          <h2 class="gb-h2">{{ s.spec_heading }}</h2>
        </div>
        <div class="gb-table-wrap">
          <table class="gb-spec">
            <tbody>
              {%- for block in section.blocks -%}
                {%- if block.type != 'spec' -%}{%- continue -%}{%- endif -%}
                {%- assign b = block.settings -%}
                <tr {{ block.shopify_attributes }}>
                  <th scope="row">{{ b.label | escape | replace: '[ ', fill_open | replace: ' ]', fill_close }}</th>
                  <td>{%- if b.value != blank -%}{{ b.value | escape | replace: '[ ', fill_open | replace: ' ]', fill_close }}{%- else -%}<span class="gb-fill">[ CONFIRM ]</span>{%- endif -%}</td>
                </tr>
              {%- endfor -%}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </section>
  {%- endif -%}
</div>

{% stylesheet %}
.gb-pb p{margin:0}
.gb-pb .gb-sec{padding:clamp(56px,7vw,100px) 0}
.gb-pb .gb-head{display:grid;gap:10px;margin-bottom:clamp(34px,4.5vw,60px)}
.gb-pb .gb-head--tight{margin-bottom:0}
.gb-pb .gb-head--center{justify-items:center;text-align:center}
.gb-pb .gb-head--center .gb-lede{max-width:52ch;text-align:center}
.gb-pb .gb-h2{font-family:var(--font-h);font-weight:700;text-transform:uppercase;
  font-size:clamp(30px,4.4vw,52px);line-height:1.06;letter-spacing:-.01em;color:var(--brown)}
.gb-pb .gb-sec--brown .gb-h2{color:var(--brown-ink)}
.gb-pb .gb-sec--night .gb-h2{color:var(--night-ink)}
.gb-pb .gb-lede{font-size:clamp(18px,1.8vw,21px);line-height:1.6;max-width:46ch;color:var(--dim);margin-top:4px}
.gb-pb .gb-eyebrow{font-size:14px;letter-spacing:.16em;margin-bottom:4px}
.gb-pb .gb-h3{font-family:var(--font-h);font-size:clamp(21px,2.1vw,26px);line-height:1.18;margin:0 0 8px;font-weight:800;color:var(--brown)}
.gb-pb .gb-sec--brown .gb-h3,.gb-pb .gb-sec--night .gb-h3{color:inherit}
.gb-pb .gb-visually-hidden{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}
.gb-pb .gb-table-wrap{overflow-x:auto}
.gb-pb a:focus-visible,.gb-pb summary:focus-visible,.gb-pb button:focus-visible{outline:3px solid var(--leaf);outline-offset:3px;border-radius:6px}
.gb-pb .gb-sec--night a:focus-visible,.gb-pb .gb-sec--brown a:focus-visible{outline-color:var(--lime)}
.gb-pb svg{width:100%;height:100%;display:block}
.gb-pb .gb-btn{min-height:60px;font-size:19px;padding:0 34px}

/* ——— specs ——— */
.gb-pb .gb-spec{width:100%;border-collapse:separate;border-spacing:0;background:var(--paper);
  border:1px solid var(--rule);border-radius:var(--radius);overflow:hidden;margin-top:26px}
.gb-pb .gb-spec th,.gb-pb .gb-spec td{padding:18px 22px;border-bottom:1px solid var(--rule);text-align:left;font-size:18px;vertical-align:top}
.gb-pb .gb-spec tr:last-child th,.gb-pb .gb-spec tr:last-child td{border-bottom:0}
.gb-pb .gb-spec th{width:38%;color:var(--dim);font-weight:700}
{% endstylesheet %}

{% schema %}
{
  "name": "GB Specs",
  "tag": "section",
  "class": "gb-section",
  "settings": [
    {
      "type": "paragraph",
      "content": "Numbers we do not have yet stay as [ PLACEHOLDERS ]. A blank value shows [ CONFIRM ]. Nothing here is invented."
    },
    {
      "type": "text",
      "id": "spec_eyebrow",
      "label": "Eyebrow",
      "default": "Specifications"
    },
    {
      "type": "text",
      "id": "spec_heading",
      "label": "Heading",
      "default": "The numbers."
    }
  ],
  "blocks": [
    {
      "type": "spec",
      "name": "Spec",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "Weight"
        },
        {
          "type": "text",
          "id": "value",
          "label": "Value",
          "info": "Blank shows [ CONFIRM ]"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "GB Specs",
      "blocks": [
        {
          "type": "spec",
          "settings": {
            "label": "Open size",
            "value": "[ OPEN SIZE ]"
          }
        },
        {
          "type": "spec",
          "settings": {
            "label": "Folded size",
            "value": "[ FOLDED SIZE ]"
          }
        },
        {
          "type": "spec",
          "settings": {
            "label": "Weight",
            "value": "[ WEIGHT ]"
          }
        },
        {
          "type": "spec",
          "settings": {
            "label": "Weight capacity",
            "value": "[ WEIGHT CAPACITY ]"
          }
        },
        {
          "type": "spec",
          "settings": {
            "label": "Pad",
            "value": "[ PAD THICKNESS ] foam, detachable"
          }
        },
        {
          "type": "spec",
          "settings": {
            "label": "Frame",
            "value": "Steel, one piece, snap-lock"
          }
        },
        {
          "type": "spec",
          "settings": {
            "label": "Colours",
            "value": "Blue, Brown"
          }
        },
        {
          "type": "spec",
          "settings": {
            "label": "Assembly",
            "value": "None"
          }
        }
      ]
    }
  ]
}
{% endschema %}
