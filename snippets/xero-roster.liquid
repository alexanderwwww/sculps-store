{%- comment -%}
  Deprecated. The tracking roster now lives directly in templates/page.track.liquid
  in the ORDERS list. Kept as an empty stub so any old {% render 'xero-roster' %}
  call cannot break a page.
{%- endcomment -%}
